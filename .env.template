# =================================================================
# Esvibox NEXUS - Server Configuration Template
# =================================================================

# --- SEGURIDAD ---
# Clave maestra de la flota (ASCII recomendado, evitar caracteres de shell como $)
# Esta clave debe ser enviada por los nodos en la cabecera X-Nexus-Key
NEXUS_API_KEY=Nexus_Secret_Key_Change_Me_2026

# Clave anterior (para permitir rotación de llaves sin downtime)
NEXUS_API_KEY_LEGACY=Old_Key_If_Exists

# --- MONITOREO ---
# IPs o rangos CIDR autorizados para ver el Dashboard /status (Separados por coma)
NEXUS_DASHBOARD_ALLOWED_IPS=127.0.0.1,192.168.1.0/24,10.8.0.0/24

# --- IDENTIDAD DEL SERVIDOR ---
# Estos valores se usan para generar el script de bootstrap inicial
REGISTRATOR_SERVER=one.esvibox.com
REGISTRATOR_PORT=80

# --- LOGGING ---
# Nivel de log del servidor (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
